# 环境变量配置示例

# 数据库配置
# 默认: app.db (当前目录)
# 可以指定绝对路径或相对路径
DATABASE_FILE=app.db

# 使用子目录
# DATABASE_FILE=data/app.db

# 使用绝对路径
# DATABASE_FILE=/Users/zhangyue/IdeaProjects/ssh/sc/data/app.db

# 服务器配置(未来可扩展)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=3000

# Session 配置(未来可扩展)
# SESSION_SECURE=false
# SESSION_MAX_AGE=3600
